# Btop Theme - Generated by theme-switcher
# Theme: {{THEME_NAME}}

# Main background
theme[main_bg]="{{BG}}"

# Main text color
theme[main_fg]="{{TEXT}}"

# Title color
theme[title]="{{PRIMARY_LIGHT}}"

# Highlight color
theme[hi_fg]="{{PRIMARY}}"

# Selected item background
theme[selected_bg]="{{PRIMARY_DARK}}"

# Selected item foreground
theme[selected_fg]="{{TEXT_BRIGHT}}"

# Inactive text
theme[inactive_fg]="{{TEXT_DIM}}"

# Progress bar gradient start
theme[proc_misc]="{{PRIMARY}}"

# Box outlines
theme[cpu_box]="{{PRIMARY}}"
theme[mem_box]="{{PRIMARY}}"
theme[net_box]="{{PRIMARY}}"
theme[proc_box]="{{PRIMARY}}"

# Graph colors (gradient from dark to light)
theme[cpu_start]="{{PRIMARY_DARK}}"
theme[cpu_mid]="{{PRIMARY}}"
theme[cpu_end]="{{PRIMARY_LIGHT}}"

theme[download_start]="{{PRIMARY_DARK}}"
theme[download_mid]="{{PRIMARY}}"
theme[download_end]="{{PRIMARY_LIGHT}}"

theme[upload_start]="{{PRIMARY_DARKEST}}"
theme[upload_mid]="{{ACCENT}}"
theme[upload_end]="{{PRIMARY_LIGHT}}"

# Memory colors
theme[used_start]="{{PRIMARY_DARK}}"
theme[used_mid]="{{PRIMARY}}"
theme[used_end]="{{PRIMARY_LIGHT}}"

theme[available_start]="{{BG}}"
theme[available_mid]="{{BORDER}}"
theme[available_end]="{{TEXT_DIM}}"

theme[cached_start]="{{PRIMARY_DARKEST}}"
theme[cached_mid]="{{PRIMARY_DARK}}"
theme[cached_end]="{{PRIMARY}}"

theme[free_start]="{{BG_DARK}}"
theme[free_mid]="{{BG}}"
theme[free_end]="{{BG_LIGHT}}"

# Temperature colors
theme[temp_start]="{{SUCCESS}}"
theme[temp_mid]="{{WARNING}}"
theme[temp_end]="#EF4444"

# Process colors
theme[process_start]="{{PRIMARY}}"
theme[process_mid]="{{ACCENT}}"
theme[process_end]="{{PRIMARY_LIGHT}}"

# Divider line
theme[div_line]="{{BORDER}}"

# Meter backgrounds
theme[meter_bg]="{{BG_DARK}}"
